// reminder.js
 require('dotenv').config(); const pool = require('./db'); const twilio = require('twilio'); // Twilio setup 
 const client = twilio(process.env.TWILIO_ACCOUNT_SID, process.env.TWILIO_AUTH_TOKEN); const FROM = process.env.TWILIO_PHONE; // Function to send SMS reminders to all users async function sendReminders() { try { const [users] = await pool.query('SELECT phone FROM users'); for (const user of users) { const phone = user.phone.startsWith('+') ? user.phone : +91${user.phone}; await client.messages.create({ body: '⚡ EnergyBuddy Reminder: Please check your devices and save power!', from: FROM, to: phone, }); console.log('✅ Reminder sent to', phone); } console.log('All reminders sent successfully!'); } catch (err) { console.error('❌ Error sending reminders:', err); } finally { process.exit(0); } } sendReminders();